{
  "name": "neuron-ai-proxy",
  "version": "1.0.0",
  "description": "Прокси для нейронного ИИ с использованием функций Vercel",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "echo 'No start script needed for Vercel'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^0.14.0"
  }
}
